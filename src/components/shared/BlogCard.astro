---
const { data } = Astro.props;
const blogHref = "/blog/";
---

<article class="blog-card">
  <div class="blog-card__image image-wrap fit-cover">
    <img src={data.image} alt="" />
  </div>
  <div class="blog-card__content">
    <div class="_date">{data.date}</div>
    <header class="_title font-bold">
      <a class="font-20-fluid" href={blogHref + data.group + "/" + data.slug}>{data.title}</a>
    </header>
    <p class="_description font-14-fluid font-light">
      {data.description}
    </p>
  </div>
</article>
